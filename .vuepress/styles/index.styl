.theme-container.homepage {
  .hero {
    img {
      width: 70%;
      object-fit: cover;
      margin: 100px 0 0 0;
      display: block;
      margin-left: auto;
      margin-right: auto;
    }
  }
  .home {
    max-width: 80%;
    padding: 0;
  }
  .features {
    padding: 0.5rem 1rem;
  }
}
